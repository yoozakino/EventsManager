<Window x:Class="EventsManager.EventEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мероприятие" Height="360" Width="560"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="ID" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="IdBox" Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>

        <TextBlock Text="Название*" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Row="2" Grid.Column="1"/>

        <TextBlock Text="Город*" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CityBox" Grid.Row="4" Grid.Column="1"
                  DisplayMemberPath="Name"/>

        <TextBlock Text="Дата (строкой)*" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="DateBox" Grid.Row="6" Grid.Column="1"
                 ToolTip="Дата"/>

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="120" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Отмена" Width="120" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
